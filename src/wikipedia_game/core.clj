(ns wikipedia-game.core

  (:require [clojure.xml :as x]
            [net.cgrand.enlive-html :as h])

  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
